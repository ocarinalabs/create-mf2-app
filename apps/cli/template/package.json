{
  "name": "mf2-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "translate": "turbo translate",
    "boundaries": "turbo boundaries",
    "bump-deps": "bun x npm-check-updates --deep -u && bun install",
    "bump-ui": "npx shadcn@latest add --all --overwrite -c packages/design-system",
    "clean": "git clean -xdf node_modules",
    "convex-lint": "turbo convex-lint",
    "env:init": "bun scripts/env.ts init",
    "env:check": "bun scripts/env.ts check",
    "env:push": "bun scripts/env.ts push",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@repo/typescript-config": "workspace:*",
    "@turbo/gen": "^2.8.20",
    "@types/node": "^25.5.0",
    "lefthook": "^2.1.4",
    "lint-staged": "^16.4.0",
    "turbo": "^2.8.20",
    "typescript": "^6.0.2",
    "ultracite": "7.3.2",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.11",
  "overrides": {
    "parse5": "^7.2.1"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc,css}": "ultracite fix"
  }
}
